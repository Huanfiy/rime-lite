# docs-rules — 文档记录与清理规则

| 项 | 内容 |
| ---- | ---- |
| 定位 | AI 辅助开发长期迭代中的信息治理规则：明确记录什么、记在哪、何时清理、怎么清理 |
| 不覆盖 | 词库数据维护（见 [docs/design/lexicon-sop.md](docs/design/lexicon-sop.md)）；git 历史管理（历史永不改写） |

## 1. 原则

- 工作区只保留两类信息：**当前事实**（现行设计、现行协议、现行流程）与**长期记录**（实测数据、优化结论等不可再生信息）。其余归 git 历史。
- 同一事实只允许一处正文，其余位置以链接指向；历史版本由 git 承担，文档不承担版本职能。
- AI 会话产出信息的速度远高于人工，默认动作是「不记录」；落档需过 §3 的记录门槛。
- 文档与代码冲突时，以代码为准修正文档；冲突表述即清理对象。

## 2. 记录框架

| 层 | 位置 | 装什么 | 生命周期 |
| ---- | ---- | ---- | ---- |
| 对外说明 | `README.md` | 项目定位、构建与使用方式 | 随功能变更更新 |
| 协作事实速查 | `AGENTS.md` | AI 协作所需的仓库现状：命令、红线、文档指针；不复述设计正文 | 随仓库现状同轮更新 |
| 拍板设计 | `docs/design/` | 现行架构、决策记录（D-n）、SOP、模块现行结构 | 就地更新，始终等于当前事实 |
| 组件说明 | `services/*/README.md` | 该组件协议与运维；不承担仓库级决策 | 随该组件变更更新 |
| 长期备忘 | `docs/notes/` | 实测数据、优化结论等不可再生信息 | 长期保留，失实即修正或清理 |
| 待实施 | `docs/plan/` | 已评审、待实施的方案；一项一文件 | 落地后搬入 `docs/design/` 并删除本文 |
| 变更记录 | git log | 变更主题、筛选口径、验证结论（一笔一主题） | 永久，不改写 |

## 3. 记录门槛

全部满足才落档，否则不记录：

1. 面向未来读者（下一轮会话、他机、他人），而非面向当前对话；
2. 无法从代码、配置、git 历史直接推导；
3. 有明确归属层（§2），且该层尚无同一事实——已有则更新原文，不另起新文。

## 4. 不记录清单

- 一次性 implementation plan：实现合入后，过程信息由 git log 承载，plan 本身不入库。已评审、待实施的方案归 `docs/plan/`，落地后按该层生命周期清理。
- AI 工具目录（`.claude/`、`.cursor/`、`.codex/`、`.agents/`、`.gemini/` 等）：属个人开发环境配置，一律不入库，统一由 `.gitignore` 隔离。
- 未决探索草稿（方案对比、推导过程）：留在已隔离位置（AI 工具目录、`tmp/` 等），拍板后按 §3 门槛落档结论。
- 可推导事实：配置内容不复述进文档（引用文件路径即可），变更历史不复述（git log 即记录）。
- 过程性对话、试错细节、被否弃的中间方案。
- 一次性中间产物（探针脚本、临时日志、生成物、候选 TSV、staging 目录）：放 `tmp/` 或仓库外，不入库。
- 运行态文件：由 `.gitignore` 隔离（`rime/build/`、`rime/*.userdb/`、`rime/sync/`、`installation.yaml`、`user.yaml` 等）。
- 本机运行态（激活工程、服务是否在位、已装包、installation_id）——以 `./run.sh status` 为准，不写入文档。
- 密钥：仅存 `~/.config/rime-candidate-daemon/config.json`（0600）；严禁写入仓库任何文件、提交说明或会话落档。

## 5. 清理准则

**触发点**（事件驱动，不设固定周期）：

- 功能合入或模块删除，导致既有文档失实；
- 设计变更拍板，旧设计描述失去「当前事实」属性；
- 发现文档表述与代码现状冲突（以代码为准修正，冲突表述即清理对象）。

**判据三问**（全「否」即清理）：

1. 它描述的还是当前事实吗？
2. 它支撑的事项还未决吗？
3. 它有 git 历史之外、不可再生的价值吗？

**清理方式**：

- 文件级：移出工作区；所有引用处改为「已移出工作区，git 历史 `<引入提交 hash>` 可查」；不留墓碑文件。
- 章节级：正文清空，保留章节号与一行墓碑说明（保持编号稳定，避免既有引用漂移）。
- 决策行（D-n）：被推翻不删行，原地标注「已被 D-m 推翻」——决策链本身是当前事实的一部分。被推翻行只保留结论与推翻指向，不保留已被替代的过程细节。
- 清理独立成笔提交（`🔧 chore`），提交说明写明清理对象与理由。
